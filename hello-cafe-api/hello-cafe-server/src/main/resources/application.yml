
server:
  port: 8080
spring:
  profiles:
    active: dev
  main:
    allow-circular-references: true

  datasource:
    driver-class-name: ${hello-cafe.datasource.driver-class-name}
    url: jdbc:mysql://${hello-cafe.datasource.host}:${hello-cafe.datasource.port}/${hello-cafe.datasource.database}?serverTimezone=UTC&useSSL=false&characterEncoding=utf8
    username: ${hello-cafe.datasource.username}
    password: ${hello-cafe.datasource.password}

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true

  security:
    user:
      name: admin
      password: admin123

logging:
  level:
    com:
      hello-cafe:
        repository: DEBUG
        service: DEBUG
        controller: DEBUG

hello-cafe:
  jwt:
    admin-secret-key: mySuperSecretKeyForAdmin1234567890
    admin-ttl: 86400000 # 24 hours in milliseconds
    admin-token-name: admin_token

    user-secret-key: mySuperSecretKeyForUser1234567890
    user-ttl: 86400000 # 24 hours in milliseconds
    user-token-name: user_token
